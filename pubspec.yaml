name: alwan
description: danmaklen.com UI.

version: 1.0.3

environment:
  sdk: ">=3.11.0 <4.0.0"
  flutter: ">=3.41.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_web_plugins:
    sdk: flutter
  dio: ^5.5.0+1
  get_it: ^9.2.1
  go_router: ^17.1.0
  provider: ^6.1.2
  json_annotation: ^4.11.0
  collection: ^1.19.1
  jwt_decoder: ^2.0.1

dev_dependencies:
  build_runner: ^2.4.12
  json_serializable: ^6.8.0

flutter:
  uses-material-design: true
